<!DOCTYPE html>
<html>
  <head>
    <title>RailsAuth</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      body {
        margin: 0;
        font-family: Arial, sans-serif;
        background: #f8f9fa;
      }
      .navbar {
        background: #343a40;
        color: #fff;
        padding: 1rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .navbar a {
        color: #fff;
        text-decoration: none;
        margin-left: 1rem;
      }
      .container {
        max-width: 500px;
        margin: 2rem auto;
        background: #fff;
        padding: 2rem;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
      }
      @media (max-width: 600px) {
        .container {
          margin: 1rem;
          padding: 1rem;
        }
        .navbar {
          flex-direction: column;
          align-items: flex-start;
        }
      }
    </style>
  </head>
  <body>
    <div class="navbar">
      <span>
        <% if user_signed_in? %>
          <%= link_to "RailsAuth", authenticated_root_path %>
        <% else %>
          <%= link_to "RailsAuth", unauthenticated_root_path %>
        <% end %>
      </span>
      <span>
        <% if user_signed_in? %>
          <a href="/users/sign_out" data-turbo-method="delete">Logout</a>
        <% else %>
          <%= link_to "Login", new_user_session_path %>
          <%= link_to "Sign Up", new_user_registration_path %>
        <% end %>
      </span>
    </div>
    <div class="container">
      <% if notice %>
        <p style="color: green;"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p style="color: red;"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>